# Dependencias del armador de APUs
openpyxl>=3.1        # lectura/escritura de Excel
anthropic>=0.40      # IA acotada (opcional; sin API key se usa fallback determinístico)
pytest>=7.0          # pruebas (solo para desarrollo)
# tkinter viene con la instalación estándar de Python en Windows.
fastapi>=0.138.0
uvicorn>=0.49.0
python-multipart>=0.0.32
httpx>=0.28.1
psycopg[binary]>=3.2   # driver Postgres (backend de nube)
psycopg-pool>=3.2      # pool de conexiones para el backend Postgres
